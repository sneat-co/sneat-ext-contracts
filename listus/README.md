# Backend contract surface

Keep Listus contract-facing Go definitions, compatibility checks, and generated-shape
verification here. Implementation handlers belong in the private `listus` repository.

Provenance: extracted from sneat-co/ext-listus, `backend/` @ tag `backend/v0.0.8`
(commit 1ad9bfa1cf995a116cd78de2a09741ee05c1f6e0). The source module path
`github.com/sneat-co/ext-listus/backend` is retired here in favour of
`github.com/sneat-co/sneat-ext-contracts/listus`; consumers move to this path at
the first tag cut from this module.
