{
  "name": "circleus-contract",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/circleus/src",
  "projectType": "library",
  "tags": ["family:circleus", "layer:contract"],
  "targets": {
    "build": {
      "executor": "@nx/angular:package",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "options": {
        "project": "libs/circleus/ng-package.json",
        "tsConfig": "libs/circleus/tsconfig.lib.json"
      },
      "configurations": {
        "production": { "tsConfig": "libs/circleus/tsconfig.lib.prod.json" }
      },
      "defaultConfiguration": "production"
    },
    "nx-release-publish": {
      "options": { "packageRoot": "dist/{projectRoot}" }
    }
  }
}
