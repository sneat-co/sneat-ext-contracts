{
  "name": "calendarius-contract",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/calendarius/src",
  "projectType": "library",
  "tags": ["family:calendarius", "layer:contract"],
  "targets": {
    "build": {
      "executor": "@nx/angular:package",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "options": {
        "project": "libs/calendarius/ng-package.json",
        "tsConfig": "libs/calendarius/tsconfig.lib.json"
      },
      "configurations": {
        "production": { "tsConfig": "libs/calendarius/tsconfig.lib.prod.json" }
      },
      "defaultConfiguration": "production"
    },
    "test": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": { "cwd": "libs/calendarius", "command": "vitest run" }
    },
    "nx-release-publish": {
      "options": { "packageRoot": "dist/{projectRoot}" }
    }
  }
}
