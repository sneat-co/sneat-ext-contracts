# @sneat/extension-debtus-contract

The `contract` tier of the **debtus** extension (extension library-architecture
convention). See the repo root `README.md` → Frontend for the tier table.

## Running unit tests

Run `npx nx test extension-debtus-contract` to execute the unit tests.
