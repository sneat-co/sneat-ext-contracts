{
  "name": "sizeus-contract",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/sizeus/src",
  "projectType": "library",
  "tags": ["family:sizeus", "layer:contract"],
  "targets": {
    "build": {
      "executor": "@nx/angular:package",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "options": {
        "project": "libs/sizeus/ng-package.json",
        "tsConfig": "libs/sizeus/tsconfig.lib.json"
      },
      "configurations": {
        "production": { "tsConfig": "libs/sizeus/tsconfig.lib.prod.json" }
      },
      "defaultConfiguration": "production"
    },
    "nx-release-publish": {
      "options": { "packageRoot": "dist/{projectRoot}" }
    }
  }
}
