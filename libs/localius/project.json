{
  "name": "localius-contract",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/localius/src",
  "projectType": "library",
  "tags": ["family:localius", "layer:contract"],
  "targets": {
    "build": {
      "executor": "@nx/angular:package",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "options": {
        "project": "libs/localius/ng-package.json",
        "tsConfig": "libs/localius/tsconfig.lib.json"
      },
      "configurations": {
        "production": { "tsConfig": "libs/localius/tsconfig.lib.prod.json" }
      },
      "defaultConfiguration": "production"
    },
    "nx-release-publish": {
      "options": { "packageRoot": "dist/{projectRoot}" }
    }
  }
}
