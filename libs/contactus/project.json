{
  "name": "contactus-contract",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/contactus/src",
  "projectType": "library",
  "tags": ["family:contactus", "layer:contract"],
  "targets": {
    "build": {
      "executor": "@nx/angular:package",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "options": {
        "project": "libs/contactus/ng-package.json",
        "tsConfig": "libs/contactus/tsconfig.lib.json"
      },
      "configurations": {
        "production": { "tsConfig": "libs/contactus/tsconfig.lib.prod.json" }
      },
      "defaultConfiguration": "production"
    },
    "test": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": { "cwd": "libs/contactus", "command": "vitest run" }
    },
    "nx-release-publish": {
      "options": { "packageRoot": "dist/{projectRoot}" }
    }
  }
}
